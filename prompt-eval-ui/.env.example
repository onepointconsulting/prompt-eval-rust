NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:3001/api

# next-auth — generate a real secret with: openssl rand -base64 32
NEXTAUTH_SECRET=replace-with-a-generated-secret
NEXTAUTH_URL=http://localhost:3000

# Dev-only placeholder credentials (verifyCredentials in src/lib/auth.ts).
# Replaced by the Rust backend in the backend step.
AUTH_DEV_EMAIL=murtaza.hassani@onepointltd.com
AUTH_DEV_PASSWORD=87654321
