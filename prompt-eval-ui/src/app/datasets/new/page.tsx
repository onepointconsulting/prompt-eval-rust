import { PageContainer } from "@/components/layout/PageContainer";
import { Card } from "@/components/ui/Card";

export default function NewDatasetPage() {
  return (
    <PageContainer title="New Dataset" description="Create or upload a new dataset.">
      <Card>New dataset workflow will be connected to backend endpoints.</Card>
    </PageContainer>
  );
}
