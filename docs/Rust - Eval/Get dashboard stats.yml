info:
  name: Get dashboard stats
  type: http
  seq: 1

http:
  method: GET
  url: http://127.0.0.1:3001/api/stats
  body:
    type: json
    data: |
      {
        "name": "My Custom Prompt Test! Updated",
        "template": "Answer the following question concisely: {question}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
